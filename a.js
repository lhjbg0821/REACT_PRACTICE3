let age = 26;

// let beverage = age >= 20 ? "Beer" : "Coke?";
let beverage = age >= 20 ? "Beer" : "Coke?";
console.log(beverage);

if (age >= 20) {
  beverage = "Beer";
} else {
  beverage = "Coke";
}
